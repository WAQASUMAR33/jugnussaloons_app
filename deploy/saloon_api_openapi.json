{
  "openapi": "3.0.0",
  "info": {
    "title": "Jugnu Saloon Public API",
    "version": "1.0.0",
    "description": "Public REST API specification for front-end integration (Products, Services, Categories, Appointment Booking, Contact Form)"
  },
  "servers": [
    {
      "url": "http://127.0.0.1:8000",
      "description": "Local Development Server"
    }
  ],
  "paths": {
    "/api/products": {
      "get": {
        "summary": "Fetch Products Catalog",
        "parameters": [
          {
            "name": "search",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful operation"
          }
        }
      }
    },
    "/api/services": {
      "get": {
        "summary": "Fetch Services Catalog",
        "parameters": [
          {
            "name": "category_id",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer"
            }
          },
          {
            "name": "search",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful operation"
          }
        }
      }
    },
    "/api/service-categories": {
      "get": {
        "summary": "Fetch Service Categories",
        "responses": {
          "200": {
            "description": "Successful operation"
          }
        }
      }
    },
    "/api/appointments": {
      "post": {
        "summary": "Book Appointment",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["customer_name", "customer_phone", "appointment_date", "service_ids"],
                "properties": {
                  "customer_name": { "type": "string" },
                  "customer_phone": { "type": "string" },
                  "customer_email": { "type": "string" },
                  "appointment_date": { "type": "string", "format": "date" },
                  "start_time": { "type": "string" },
                  "service_ids": { "type": "array", "items": { "type": "integer" } },
                  "notes": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Appointment created"
          },
          "422": {
            "description": "Validation error"
          }
        }
      }
    },
    "/api/contact": {
      "post": {
        "summary": "Submit Contact Form",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["name", "email", "message"],
                "properties": {
                  "name": { "type": "string" },
                  "email": { "type": "string" },
                  "phone": { "type": "string" },
                  "subject": { "type": "string" },
                  "message": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Contact message submitted"
          },
          "422": {
            "description": "Validation error"
          }
        }
      }
    }
  }
}
